apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: development

spec:
  selector:
    app: nginx

  ports:
    - protocol: TCP
      port: 82        # Cluster / host port [service]
      targetPort: 80  # Container port

      